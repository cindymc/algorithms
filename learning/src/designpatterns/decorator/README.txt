A decorator is the same type as the object it is decorating (eg, DarkRoast is-a Beverage, and Mocha is-a Beverage).

The decorator adds its own behavior either before and/or after delegating to the object it decorates to do the rest
of the job.