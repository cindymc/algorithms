The Template pattern uses an abstract base class to implement common functionality (algorithms) in a base class, then
lets subclasses either override those, and/or add detailed methods.

Subclasses can refine some steps of the algorithm without changing the algorithm's structure.